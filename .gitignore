/.venv/
/.pytest_cache/
/.mypy_cache/
/.cache/
act.exe
act.zip
.DS_Store
*.pyc
__pycache__/# Virtual environments
.venv/

# Python cache
__pycache__/
*.pyc

# IDE/editor
.vscode/
*.swp
.DS_Store
Thumbs.db

# Build outputs
build/
dist/
*.egg-info/
.pytest_cache/

# Tool caches
.ruff_cache/
.mypy_cache/
.coverage
coverage.xml
htmlcov/

# Local env
.env

# Local notes and experiments
# (project drafts moved to `private_docs/` — kept local and not versioned)
*.local
*.draft

# Local-only private documentation (project drafts)
# Keep `private_docs/` local; it replaces `docs/drafts/` as the working draft area.
private_docs/

# Ignore the working architecture diagram (not ready to be versioned)
/docs/diagrams/architecture.mmd

# Local artifacts (don't version binary DBs or large zips)
/docs/artifacts/assist_run/*.db
/docs/artifacts/assist_run/*.zip
/docs/artifacts/assist_run/*.tar.gz

# Local scratch DB (created during assisted runs)
demo.db

# Temporary folders used by local CI/demo smoke runs
/.tmp_ci_artifacts/
/.tmp_ci_smoke/

# Local generated / junk files (ignore if created during demos/tests)
# SQLite / demo DBs
*.db
*.sqlite
*.sqlite3

# k6 downloaded utils and scripts
scripts/k6/
scripts/k6/*.js

# logs and runtime artifacts
*.log
logs/

# editor swap / temp (already included but ensure coverage)
*~

